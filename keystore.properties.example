# Copy to keystore.properties for a local signed release build.
# The actual keystore and this file are ignored by Git.
storeFile=path/to/jingdu-release.jks
storePassword=replace-me
keyAlias=jingdu
keyPassword=replace-me
